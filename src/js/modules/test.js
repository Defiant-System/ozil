
let Test = {
	init(APP) {
		// return;

		// setTimeout(() => APP.content.find(`.controls span[data-click="toggle-menu"]:nth(0)`).trigger("click"), 400);
		// setTimeout(() => APP.content.find(`span[data-click="menu-go-sub"]:nth(0)`).trigger("click"), 900);
		return;
	}
};
